##############################################
        userprofile:

Profile
GET - только для владельца профиля (request.user==Profile.user)
PUT - только для владельца профиля (request.user==Profile.user)

##############################################
        company:

Company - только для группы owner
WorkerProfile - только для группы owner

##############################################
        main:

Country - не трогать
City - не трогать

Warehouse - только для группы owner и поле компании предзаполнено
Условие для всех: Имеет request.user.workerprofile
GET - Фильтрация company__workerprofile__user=self.request.user и имеет request.user.workerprofile
POST - Подставить компанию workerprofile__user=self.request.user и имеет request.user.workerprofile
PUT - Подставить компанию workerprofile__user=self.request.user и проверить request.user.workerprofile.company == object.company
DELETE - Проверить request.user.workerprofile.company == object.company


Transport -
Sending -

Order -
Application -
